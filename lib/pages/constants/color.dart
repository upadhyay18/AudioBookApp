import 'package:flutter/material.dart';

const Color kFont = Colors.black87;
const Color inActiveColor = Color.fromARGB(157, 0, 0, 0);
const Color activeColor = Color.fromARGB(255, 10, 10, 10);
const Color bgBlack = Color.fromARGB(224, 0, 0, 0);

const BoxShadow bookAppShadow = BoxShadow(
    color: Color.fromARGB(189, 39, 39, 39),
    offset: Offset(0, 30),
    blurRadius: 50);
